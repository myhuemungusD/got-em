# Copy this file to `.env.local` and fill in values from your Firebase project.
# Firebase console → Project settings → Your apps → </> → register web app →
# copy each value below. `.env.local` is gitignored.
#
# Leave all values empty during local dev to run in TEST_MODE (in-memory mock).

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_STORAGE_BUCKET=
